# 304 — Честный экспорт-бэкап (опционально)

Модель: sonnet · Волна: 4 · Зависит от: — · Статус: отложен, брать после 401/402

## Контекст

`settings.ui.tsx:84-92` экспортирует клиентский `AppData` как «бэкап», но это
RBAC-отфильтрованный снапшот текущего актора — неполный для не-владельца.

## Файлы

- `settings.ui.tsx` — изменить (подпись + условие).
- `server/index.ts`/routes — изменить (новый GET `/api/family/export`, ownerOnly).

## Точные изменения

1. Подпись кнопки/описания: «Экспорт данных, видимых вашей роли».
2. Для OWNER — отдельная кнопка «Полный бэкап семьи»: GET `/api/family/export` (policy ownerOnly) возвращает полный family-scoped envelope (все коллекции без фильтрации, БЕЗ server-секретов) как attachment JSON.

## Тесты

- router.test.ts: export ownerOnly (CHILD → 403), полнота коллекций.

## Acceptance criteria / Не трогать / Самопроверка

По образцу остальных: `npm run check` зелёный; не трогать механику экспорта клиентского JSON (остаётся как есть для всех ролей).
