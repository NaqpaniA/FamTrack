# 303 — Переименовать «Вишлист» в финансах

Модель: haiku · Волна: 4 · Зависит от: —

## Контекст

`finance.ui.tsx:821` называет секцию SavingsGoal «Вишлист», при этом настоящий
wishlist-виджет называется «Желания» (`household.ui.tsx:571`). Коллизия имён
двух разных доменов.

## Файлы

- `finance.ui.tsx` — изменить (только строковые литералы секции SavingsGoal).

## Точные изменения

1. Заголовок секции «Вишлист» → «Цели накоплений». Проверить соседние тексты (кнопки/пустые состояния/тосты этой секции) на слово «вишлист» и заменить на «цель накоплений» в нужном падеже: `grep -n "ишлист" finance.ui.tsx store.ts`.

## Тесты

Существующие зелёные (текст в них не завязан — проверить grep'ом «Вишлист» в *.test.tsx; если завязан — поправить ожидания).

## Acceptance criteria

- `grep -rn "Вишлист" --include="*.tsx" . --exclude-dir=node_modules` → 0 вне household-домена.
- `npm run check` зелёный.

## Не трогать

- household.ui.tsx («Желания» остаётся), модели, server.

## Самопроверка

- [ ] Ни один функциональный идентификатор не переименован — только строки UI.
