# 206 — Миграция: family

Модель: haiku · Волна: 3 · Зависит от: 202, 203, 112 · Шаблон: `2xx-screen-migration-template.md` (прочитать первым)

## Файлы

- `family.ui.tsx` — изменить.

## Специфика экрана (поверх шаблона)

- 8 копий инпут-рецепта (`:435-505`) → `Input`/`Select` из ui-kit (самая большая экономия тикета).
- Карточки участников/наград `:42,80,111` `rounded-[14px]` → `rounded-card`.
- Truncate обязателен: имя участника `:52` (`truncate` + `min-w-0` на родителе), название награды `:82` (`line-clamp-2 break-words` — карточка фикс-высоты `min-h-[150px]`), имя в лидерборде `:777` (`truncate`).
- Чёрная карточка баланса магазина `:836` `bg-black text-white` → `bg-app-surface-strong text-white`.
- Радужный градиент `:764` (`from-indigo-500 via-purple-500 to-pink-500`) — единственный в приложении: заменить на `bg-app-accent` (однотонный премиум вместо радуги). Если визуально беднеет — допустим `bg-gradient-to-r from-app-accent to-app-accent/70`.
- Декоративный blob `:71` (absolute w-32 h-32) — удалить (одноразовый флюриш, вне системы).
- Native confirm в `:545,558,728` НЕ трогать — тикет 211.

## Grep-гейты, AC, «Не трогать», самопроверка

Из шаблона, применительно к `family.ui.tsx`. Если тикет 112 внёс селектор «Кому» — мигрировать его классы тоже, поведение не трогать.
