# 212 — Тосты стеком

Модель: haiku · Волна: 3 · Зависит от: —

## Контекст

`store.ts:60-66` `addToast` заменяет массив целиком (`setToasts([{...}])`) —
виден максимум один тост, новый обрубает предыдущий. `ToastContainer`
(ui-kit.tsx:321) уже готов рендерить список.

## Файлы

- `store.ts` — изменить (только addToast/removeToast-логика).
- `store-toasts.test.tsx` или дополнение к существующему подходящему тесту — создать.

## Точные изменения

1. `addToast`: `setToasts(prev => [...prev, next].slice(-3))` (максимум 3, старые вытесняются); таймер удаления по id оставить как есть (он уже фильтрует по id).
2. Убедиться, что `removeToast`/фильтрация не мутирует чужие таймеры.

## Тесты

- Два addToast подряд → оба в DOM; по истечении таймера первого — остаётся второй.
- Четвёртый тост вытесняет первый.

## Acceptance criteria

- `npm run check` зелёный; существующие тесты зелёные.

## Не трогать

- ui-kit.tsx, остальной store.

## Самопроверка

- [ ] Быстрая серия действий (3 сохранения подряд) показывает все тосты.
