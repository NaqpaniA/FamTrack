# 204 — Миграция: dashboard

Модель: haiku · Волна: 3 · Зависит от: 202, 203 · Шаблон: `2xx-screen-migration-template.md` (прочитать первым)

## Файлы

- `dashboard.ui.tsx` — изменить.

## Специфика экрана (поверх шаблона)

- Тайлы «БАЛАНС / К ДЕЛУ / УР.» (`:221-229`): подписи `text-[9px] font-black` → `text-caption font-bold` (11px допустимо, 9px запрещён).
- Заголовок экрана `:204` `text-[24px]` → `text-display`.
- Секционные заголовки `:116` — заменить inline-h2 на `SectionHeader` из ui-kit.
- `EPIC_COLOR_CLASSES` allowlist (`:27-39`) НЕ трогать — это защитный паттерн.
- Карточки «На повестке» — `line-clamp-2` уже есть, сохранить.

## Grep-гейты, AC, «Не трогать», самопроверка

Из шаблона, применительно к `dashboard.ui.tsx`.
